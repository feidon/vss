## 1. Reposition and restyle back navigation

- [x] 1.1 Change the header layout in `schedule-editor.ts` template: replace the single flex row with two stacked elements — a small text link on top, the service name heading below
- [x] 1.2 Restyle the back link from button appearance (`rounded bg-gray-200 px-4 py-2`) to a subtle text link (`text-sm text-gray-500 hover:text-gray-700`)

## 2. Verify

- [x] 2.1 Run `ng build` to verify no template errors
- [ ] 2.2 Visually confirm the layout in the browser (back link above heading, subtle styling)
