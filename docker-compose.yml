services:
  postgres:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: vss
      POSTGRES_PASSWORD: vss
      POSTGRES_DB: vss
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vss"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      DB: postgres
      DATABASE_URL: postgresql+asyncpg://vss:vss@postgres:5432/vss
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  pgdata:
